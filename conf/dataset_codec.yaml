# Dataset config for codec training (WebDataset with pre-extracted features).
#
# Tar structure per sample:
#   {id}.json                -- metadata
#   {id}.mel.npz             -- precomputed mel spectrogram
#   {id}.whisper_feature.npy -- pre-extracted Whisper feature (T, 1280)
#   {id}.wavlm_feature.npy  -- pre-extracted WavLM feature  (T, 1024)
#   {id}.muq_feature.npy    -- pre-extracted MuQ feature    (T, 1024)

urls: "/mnt/fcl-jfs/music_tokenizer/webdataset_data/processed_webdataset_1000w_with_features/shard_64*.tar"

max_frames_50hz: 1500
seed: 42
